# ua_debug_view example

Demo app exercising every feature of `ua_debug_view`: all 9 built-in modules,
the `DebugAccountPicker` on a fake login screen, network capture, and the
FAB / multi-tap / long-press / shake triggers.

Run it:

```sh
cd example
flutter run
```
