/// Default maximum number of network requests kept in memory.
const int kDefaultMaxRequests = 100;

/// Default maximum number of log entries kept in memory.
const int kDefaultMaxLogs = 500;

/// Default number of taps needed to trigger [DebugTrigger].
const int kDefaultTapCount = 5;

/// Duration of the bottom sheet open/close animation.
const Duration kBottomSheetAnimationDuration = Duration(milliseconds: 300);
